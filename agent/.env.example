# 内部服务密钥：Java 等接入方放在请求头 X-API-Key
AGENT_API_KEY=dev-agent-key

AGENT_HOST=0.0.0.0
AGENT_PORT=8090

# 向量库（pnpm dev 会起 ai-postgres，并默认注入该 URL）
AGENT_DATABASE_URL=postgresql://blog:blog@localhost:5433/blog_ai

# LLM（OpenAI 兼容接口，密钥只放在本服务）
# AGENT_CHAT_BASE_URL=https://llm-wtf8um994avwt8ik.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
# AGENT_CHAT_API_KEY=
# AGENT_CHAT_MODEL=deepseek-chat
# AGENT_EMBED_BASE_URL=
# AGENT_EMBED_API_KEY=
# AGENT_EMBED_MODEL=text-embedding-3-small
# AGENT_EMBED_DIMENSIONS=1536
