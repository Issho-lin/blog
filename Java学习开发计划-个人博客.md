# Java 学习开发计划：个人博客项目

## 1. 计划说明

### 1.1 学习目标

通过完整开发一个可上线的个人博客，掌握 Java 后端开发的核心能力：

- Java 语法、面向对象、集合、异常、泛型、IO 和常用标准库。
- Maven 项目管理、Git 协作和基本工程规范。
- Spring Boot 应用开发、配置管理、依赖注入和分层架构。
- HTTP、REST API、参数校验、统一异常处理和接口安全。
- PostgreSQL 数据建模、SQL、事务、索引和 JPA 持久化。
- 登录认证、权限控制、密码安全和常见 Web 安全问题。
- Markdown 解析、文件上传、文章导入导出和内容安全过滤。
- 单元测试、集成测试、日志、监控、Docker 部署和故障排查。

前端不是本计划的零基础学习主线，但仍然属于项目交付范围。你已有前端开发经验，因此前端会按实际项目需要完成页面开发、状态管理、接口联调和最终验收，并对关键实现思路做说明；本计划的学习重点仍然是 Java 后端和前后端分离场景下的服务端工程能力。

### 1.2 建议投入

- 每周 10-12 小时：每周 5 天，每天 1.5-2 小时。
- 每个工作日建议分配：30% 学习概念，60% 编码，10% 整理笔记和提交 Git。
- 全部 MVP 预计 16-20 周；如果每天投入 3 小时，可压缩至 10-14 周。
- 每个阶段都必须留下可运行成果，不把学习集中到项目最后。

### 1.3 默认技术路线

| 领域 | 首期选择 | 选择理由 |
| --- | --- | --- |
| JDK | Java 21 LTS | 版本稳定，支持现代 Java 语法 |
| 后端 | Spring Boot 3.x | Java Web 主流框架，生态成熟 |
| 构建 | Maven | 依赖、编译和测试流程清晰 |
| 数据库 | PostgreSQL | 关系模型完整，适合学习 SQL 和事务 |
| 缓存与 Session | Redis | 学习缓存、Session、TTL、计数和限流；不作为主数据库 |
| ORM | Spring Data JPA / Hibernate | 先提高开发效率，再补 SQL 原理 |
| 前端 | Next.js + React + TypeScript | 主流 React 框架，支持公开博客 SSR/SEO；前端需要正常实现，但不作为零基础学习主线 |
| 后端接口 | Spring MVC REST API | 与前端完全分离，统一使用 JSON 接口 |
| Markdown 编辑器 | 成熟的浏览器编辑器组件 | 不自行实现编辑器内核 |
| Markdown 解析 | CommonMark/GFM 兼容 Java 库 | 统一服务端渲染和安全处理 |
| 测试 | JUnit 5 + Mockito + Spring Boot Test | 覆盖单元、接口和集成测试 |
| 数据库迁移 | Flyway | 让表结构变更可追踪、可重复部署 |
| 部署 | Docker Compose | 降低本地和服务器环境差异 |

首期引入 Redis，但只用于 Session、文章详情缓存、阅读数和限流。暂不引入微服务、消息队列和 JWT 认证；认证先使用 Spring Security + HttpOnly Cookie Session。

### 1.4 前后端分离架构

```text
Next.js 前端（开发端口 3000）
        │ HTTPS / JSON / Cookie
        ▼
Spring Boot REST API（开发端口 8080）
        │
        ├── PostgreSQL：用户、文章、分类、标签、版本
        ├── 文件存储：图片和导入资源
        └── Markdown 服务：解析、清洗、导出
```

架构约定：

- 前端不直接访问数据库，所有业务操作通过后端 API 完成。
- 后端不返回 Thymeleaf 页面，Controller 只负责 JSON 响应和 HTTP 状态码。
- 公开阅读接口和作者管理接口分组，例如 `/api/public/**`、`/api/admin/**`。
- 前端通过环境变量配置 API 地址，不能把后端地址硬编码在业务代码中。
- 本地开发配置明确允许的前端 Origin；生产环境只允许正式域名。
- 使用 HttpOnly、Secure、SameSite 配置 Cookie，跨域请求由前端携带 credentials，后端配置 CORS 和 CSRF 防护。
- 先定义接口请求、响应、错误码和分页结构，再分别开发前后端。

### 1.5 后端 API 分组

| 分组 | 示例接口 | 访问者 |
| --- | --- | --- |
| 公共内容 | `GET /api/public/posts` | 匿名访客 |
| 公共详情 | `GET /api/public/posts/{slug}` | 匿名访客 |
| 认证 | `POST /api/auth/login`、`POST /api/auth/logout` | 匿名/已登录 |
| 管理文章 | `GET/POST/PATCH /api/admin/posts` | 作者 |
| 发布操作 | `POST /api/admin/posts/{id}/publish` | 作者 |
| 媒体文件 | `POST /api/admin/media` | 作者 |
| 导入导出 | `POST /api/admin/imports`、`GET /api/admin/posts/{id}/export` | 作者 |

错误响应建议统一为：`code`、`message`、`fieldErrors`、`requestId`。分页响应统一包含 `items`、`page`、`pageSize`、`total`。

## 2. 学习与开发原则

1. 先理解问题，再使用框架。每次使用 Spring 注解，都要知道它解决了什么问题。
2. 项目初始化、依赖接入和基础能力实现优先采用主流方法，例如 IntelliJ IDEA、Spring Initializr、Next.js 官方脚手架、Spring 官方方案和成熟组件库；不要用纯手工逐文件搭建来替代标准工程化流程。
3. 每个功能先写最小版本，再增加校验、异常处理和测试。
4. 先写能运行的垂直切片，例如“新建文章 -> 保存数据库 -> 页面展示”，不要先搭建所有空模块。
5. 每个阶段有明确验收条件，未通过就不进入下一阶段。
6. 每完成一个可独立运行的功能就提交一次 Git，提交信息说明行为变化。
7. 遇到报错先阅读堆栈中最靠后的 `Caused by`，再定位自己的代码，不直接复制修复方案。
8. 所有用户输入都视为不可信，包括 Markdown、文件名、URL 和 Front Matter。
9. 保持“原始内容可恢复”：文章 Markdown 原文是事实来源，渲染 HTML 只是缓存或展示结果。

## 3. 项目阶段总览

| 阶段 | 周期 | 学习主题 | 项目成果 |
| --- | --- | --- | --- |
| 0 | 第 1 周 | 开发环境与 Git | 空项目可编译、可测试、可提交 |
| 1 | 第 2-3 周 | Java 基础 | 命令行文章管理小程序 |
| 2 | 第 4 周 | Java Web 与 HTTP | Spring Boot 健康检查和文章查询接口 |
| 3 | 第 5-6 周 | Spring 分层与数据库 | 文章 CRUD REST API |
| 4 | 第 7 周 | 数据校验与异常 | 稳定的文章接口和错误响应 |
| 5 | 第 8-9 周 | Markdown 服务与自动保存 | 编辑、预览、自动保存 API |
| 6 | 第 10 周 | 登录认证与权限 | 作者后台登录和接口保护 |
| 7 | 第 11 周 | 发布与公开阅读 API | 文章发布、公开查询和归档 API |
| 8 | 第 12 周 | 分类、标签与搜索 | 内容组织和搜索 |
| 9 | 第 13 周 | 文件上传与导入导出 | Markdown 导入和文章导出 |
| 10 | 第 14-15 周 | 测试与重构 | 自动化测试和可维护结构 |
| 11 | 第 16 周 | Docker 与上线 | 可部署的 MVP |
| 12 | 后续 | 性能与扩展 | SEO、RSS、统计、版本管理等 |

## 4. 分阶段详细计划

## 阶段 0：环境、Git 与项目骨架

### 学习目标

- 安装并理解 JDK、IDE、Maven、PostgreSQL 和 Git。
- 理解 Java 项目的目录结构、编译、测试和运行流程。
- 学会从命令行查看日志和定位端口问题。

### 开发任务

1. 初始化 Git 仓库。
2. 创建 Spring Boot 项目，加入 Web、Validation、JPA、PostgreSQL、Flyway 和 Test 依赖。
3. 创建第一个 `/api/health` JSON 接口，返回应用状态。
4. 添加 `README.md`，记录启动命令、环境变量和当前功能。
5. 添加 `.gitignore`，确保密码、数据库文件和构建产物不提交。

### 验收标准

- 执行 `mvn test` 成功。
- 执行 `mvn spring-boot:run` 后可以访问健康检查地址。
- 新电脑按照 README 能启动项目。
- Git 历史至少包含“初始化项目”和“添加健康检查”两次有意义提交。

## 阶段 1：用纯 Java 做文章管理小程序

### 学习目标

- 掌握类、接口、封装、继承、多态和组合。
- 掌握 `List`、`Map`、`Set`、泛型、枚举、`Optional` 和 Stream 基础。
- 掌握异常设计、文件读写和 JSON 之外的简单文本持久化。
- 学会编写可测试的方法，而不是把逻辑全部放在 `main` 中。

### 开发任务

在 `learning/java-basics` 目录或独立分支实现一个命令行版本：

- `Post` 类：标题、slug、正文、状态、创建时间。
- `PostRepository` 接口和基于内存的实现。
- 创建、查询、修改、删除文章。
- 按标题关键字筛选文章。
- 使用枚举表示草稿、已发布、已下线。
- 将文章保存为本地文本文件，并在程序启动时加载。
- 为 slug 校验、状态转换和搜索方法编写 JUnit 测试。

### 重点练习

- 为什么状态应该使用枚举而不是字符串。
- 为什么 Repository 应该是接口。
- 什么情况应该抛出业务异常。
- 可变对象、不可变对象和时间类 `Instant`/`LocalDateTime` 的区别。

### 验收标准

- 不使用静态全局变量保存全部业务状态。
- 业务逻辑可以脱离命令行单独测试。
- 重启程序后文章仍能读取。
- 至少有 10 个测试用例，覆盖正常、空输入、重复 slug 和非法状态转换。

## 阶段 2：Java Web、HTTP 与 Spring Boot 入门

### 学习目标

- 理解浏览器、HTTP 请求、响应、状态码、Cookie 和 JSON。
- 掌握 REST Controller、路由、参数绑定和响应序列化。
- 理解 Spring 容器、Bean、依赖注入和配置文件。
- 理解跨域请求、CORS、Cookie credentials 和前后端接口契约。

### 开发任务

1. 添加 `PostController`，先使用内存数据返回文章列表和详情 JSON。
2. 实现 `GET /api/public/posts` 和 `GET /api/public/posts/{slug}`。
3. 学习 GET、POST、PATCH、DELETE、分页和 JSON 请求体。
4. 添加全局站点名称配置，例如 `blog.site-name`。
5. 将控制器中的业务逻辑移到 `PostService`。
6. 使用 DTO 传递请求和响应，不直接暴露数据库实体。
7. 配置本地前端 Origin 的 CORS，并使用 curl 或前端现有工具完成一次联调。

### 验收标准

- API 能返回文章列表和文章详情 JSON。
- API 能接收 JSON 创建文章并返回资源信息。
- Controller 不直接操作集合或数据库。
- 前端可以跨端口调用 API，且错误响应结构稳定。
- 至少写两个 `MockMvc` 接口测试。

## 阶段 3：数据库、JPA 与文章 CRUD

### 学习目标

- 掌握表、主键、唯一约束、索引、关联和基本 SQL。
- 理解 ORM、实体、Repository、事务和懒加载。
- 学会使用 Flyway 管理数据库版本。

### 开发任务

1. 启动 PostgreSQL，创建开发数据库。
2. 编写第一版 Flyway migration：`users`、`posts`、`categories`、`tags` 和关联表。
3. 将内存 Repository 替换为 Spring Data JPA Repository。
4. 完成文章新建、查询、编辑和软删除。
5. 添加 `PostStatus` 枚举和状态转换逻辑。
6. 在 `PostService` 的写操作上使用事务。
7. 为 `slug` 添加数据库唯一约束。
8. 使用分页查询文章列表。

### 重点练习

- Entity 与 DTO 为什么要分开。
- 数据库唯一约束和 Java 代码校验有什么区别。
- 事务边界为什么应放在 Service 层。
- JPA 查询方法、JPQL 和原生 SQL 的适用场景。

### 验收标准

- 删除文章后数据库记录仍保留，但公开查询不到。
- 重复 slug 在数据库层面也无法写入。
- 文章列表使用分页，不一次加载全部文章。
- 数据库删除后重新执行 migration 可以恢复结构。

## 阶段 4：校验、异常与 API 设计

### 学习目标

- 掌握 Bean Validation、统一异常处理和 API 错误结构。
- 理解 400、401、403、404、409、500 的区别。
- 学会区分参数错误、资源不存在、业务冲突和系统异常。

### 开发任务

- 为标题、正文、slug、摘要和文件大小增加校验。
- 创建统一错误响应：错误码、消息、字段错误和请求 ID。
- 使用 `@ControllerAdvice` 统一处理异常。
- 对重复 slug 返回 409。
- 对不存在文章返回 404。
- 对无权限操作返回 403，不泄露敏感数据。
- 为文章接口整理 API 文档，记录请求、响应和错误示例。

### 验收标准

- 前端可以根据字段错误定位表单问题。
- 未知异常不会把堆栈返回给用户，但日志中保留排查信息。
- 所有核心接口都有成功和失败测试。

## 阶段 5：Markdown 服务、预览与自动保存 API

### 学习目标

- 理解独立前端编辑器与后端 Markdown 服务之间的数据流。
- 掌握服务端 Markdown 解析和 HTML 清洗。
- 理解防抖、幂等、版本号和并发编辑冲突。

### 开发任务

1. 设计编辑器所需 API：获取文章、保存草稿、预览 Markdown、查询保存状态。
2. 服务端保存 Markdown 原文，并返回渲染后的安全 HTML。
3. 实现保存接口：创建草稿、更新草稿、返回保存时间和版本号。
4. 约定前端 1 秒防抖自动保存的请求格式和幂等行为；前端需按该约定实现自动保存，并明确说明页面状态流转。
5. 设计保存失败、网络断开和离开页面时前端需要处理的错误码。
6. 使用 `version` 或 `updated_at` 检测并发修改。
7. 实现代码高亮、图片展示、表格和目录生成所需的服务端数据。
8. 用接口测试验证 Markdown 解析和预览结果。

### 安全重点

- 不把未经清洗的 Markdown HTML 直接输出到页面。
- 过滤脚本标签、事件属性、危险协议和不允许的 HTML。
- 通过服务端重新渲染验证客户端预览结果。

### 验收标准

- 前端重新请求文章详情后内容不丢失。
- 保存接口失败时返回稳定错误码，前端可以重试，且服务端不会覆盖新版本。
- 两个窗口修改同一文章时，后端能检测版本冲突并返回 409。
- 输入恶意 HTML 后，API 返回的渲染内容不会执行脚本。

## 阶段 6：登录认证与权限

### 学习目标

- 理解认证和授权的区别。
- 掌握密码哈希、Session/Cookie、过滤器、CORS 和 CSRF 基础。
- 学会在服务端保护资源，而不是只在前端隐藏按钮。
- 理解前后端分离场景下 HttpOnly Cookie 的请求流程。

### 开发任务

1. 实现作者初始化或注册流程。
2. 使用安全哈希保存密码，禁止明文密码。
3. 使用 Spring Security 实现登录、退出和登录态过期，返回 JSON 而不是页面跳转。
4. 保护 `/api/admin/**` 和文章管理接口。
5. 实现访客只能查看已发布公开文章。
6. 对草稿、下线文章和回收站文章做服务端权限判断。
7. 增加登录失败限流和基础安全日志。

### 验收标准

- 未登录调用管理 API 返回 401，由前端负责跳转登录页。
- 访客不能通过修改 URL 或请求参数访问草稿。
- 密码字段不会出现在日志和接口响应中。
- 登录成功、失败、退出和越权访问都有测试。
- 前端跨域请求可以正确携带 Cookie；不允许的 Origin 会被拒绝。

## 阶段 7：发布流程与公开阅读端

### 学习目标

- 理解后台管理 API 与公开内容 API 的边界。
- 掌握状态机、发布版本和缓存失效思路。
- 设计适合 Next.js 公开端 SSR/SEO 使用的内容接口。

### 开发任务

- 实现草稿发布、已发布文章更新发布、下线和恢复。
- 发布前校验标题、正文和 slug。
- 完成首页、文章详情、归档所需的公开查询 API；页面由前端实现。
- 返回目录、阅读时长、摘要和分享元信息所需字段。
- 约定前端用于生成 title、description、canonical、Open Graph 的字段。
- 记录文章阅读数，先采用简单可靠的统计方式。

### 验收标准

- 发布前的编辑修改不会影响线上版本。
- 下线文章的公开 API 不返回正文，详情接口返回 404 或约定的不可见状态。
- 公开接口返回的数据足够前端完成桌面端、移动端和 SSR 页面。
- SEO 所需标题、摘要、规范链接和分享图字段齐全。

## 阶段 8：分类、标签与搜索

### 学习目标

- 掌握多表关联、分页、排序和查询条件组合。
- 理解数据库搜索与全文搜索的差异。

### 开发任务

- 实现分类和标签的增删改查。
- 文章支持一个分类和多个标签。
- 完成分类页、标签页和归档页。
- 支持按标题、摘要和正文关键词搜索。
- 增加搜索分页、无结果状态和 SQL 索引。

### 验收标准

- 分类、标签名称和 slug 不重复。
- 已下线、草稿文章不会出现在公开搜索结果。
- 搜索结果分页稳定，输入特殊字符不会导致 SQL 错误。

## 阶段 9：Markdown 导入、图片上传与文章导出

### 学习目标

- 掌握 `MultipartFile`、流式读写、文件校验和对象存储抽象。
- 掌握 YAML Front Matter 解析、字段映射和容错处理。
- 理解下载响应的 Content-Type、Content-Disposition 和 UTF-8 编码。

### 开发任务

1. 实现 `.md`/`.markdown` 文件上传和大小校验。
2. 解析 Front Matter，回填标题、slug、摘要、分类、标签和发布时间。
3. 导入结果创建为草稿，不自动公开。
4. 处理重复 slug、非法日期、未知字段、空文件和乱码。
5. 实现图片上传并把资源 URL 插入 Markdown。
6. 实现单篇文章导出为 UTF-8 `.md` 文件。
7. 在导出文件中生成 Front Matter。
8. 用导出文件重新导入，验证正文和元数据可恢复。

### 安全重点

- 不使用用户上传的原始文件名直接拼接服务器路径。
- 校验 MIME、扩展名、大小和文件内容。
- 资源保存路径与公开 URL 解耦。
- 禁止上传可执行脚本文件。

### 验收标准

- 合法 Markdown 可以导入并进入编辑页。
- 导入中的 `status: published` 不会绕过发布流程。
- 导出的文件能被常见 Markdown 编辑器打开。
- 非法文件不会覆盖文章，也不会让服务崩溃。

## 阶段 10：测试、重构与工程质量

### 学习目标

- 掌握测试金字塔、Mock、测试数据库和接口测试。
- 学会从重复代码、过长方法和错误分层中识别重构点。
- 掌握日志级别、配置隔离和基本代码审查。

### 开发任务

- 为 slug、状态转换、Front Matter 解析和权限判断编写单元测试。
- 使用 Spring Boot Test 测试 Controller、Service 和 Repository。
- 使用 Testcontainers 或独立测试数据库验证 PostgreSQL 行为。
- 覆盖登录、自动保存、发布、下线、导入、导出和上传失败流程。
- 增加数据库 migration 的启动测试。
- 配置格式化、静态检查和测试命令。
- 重构重复的响应、异常和权限代码。

### 建议最低覆盖范围

- 业务核心类：80% 以上行覆盖率作为参考，不以数字替代有效场景。
- 核心接口：每个接口至少覆盖成功、参数错误和权限错误。
- 导入导出：正常文件、空文件、超限文件、非法 Front Matter、中文内容。

## 阶段 11：Docker、部署与上线

### 学习目标

- 理解构建产物、容器、环境变量、反向代理和 HTTPS。
- 学会查看线上日志、数据库连接和健康状态。

### 开发任务

1. 编写多阶段 Dockerfile 构建 Spring Boot 应用。
2. 使用 Docker Compose 编排应用和 PostgreSQL。
3. 将数据库连接、密钥、上传目录和站点地址改为环境变量。
4. 配置生产日志格式、健康检查和优雅关闭。
5. 部署到一台云服务器或本地可访问环境。
6. 配置域名、HTTPS、备份和恢复演练。
7. 完成上线前安全检查和回滚说明。

### 验收标准

- 新环境执行一组明确命令即可启动应用和数据库。
- 应用重启后文章和图片仍然存在。
- 数据库可以备份并恢复出可用文章。
- 生产环境不使用开发密码和调试模式。

## 5. 每周学习节奏模板

每周按以下节奏执行：

| 时间 | 内容 |
| --- | --- |
| 第 1 天 | 学习本周概念，整理 5-10 条自己的笔记 |
| 第 2 天 | 跟随最小示例编码，不直接复制完整项目 |
| 第 3 天 | 将知识点接入博客的一个垂直切片 |
| 第 4 天 | 补充异常、边界情况和测试 |
| 第 5 天 | 重构、写 README、提交 Git 并复盘 |

每周复盘回答：

- 本周我能否不看教程解释这个功能的数据流？
- 哪些代码是复制的，哪些代码是自己理解后写的？
- 本周新增了哪些测试？它们捕获了什么风险？
- 如果删除一个关键依赖，我是否理解它替代了什么工作？
- 下周需要补哪一个 Java 或数据库基础？

## 6. 推荐的代码目录演进

```text
blog/
├── backend/
│   └── src/main/java/com/example/blog/
└── frontend/
    ├── app/
    ├── components/
    └── lib/api/
```

后端业务模块建议展开为：

```text
src/main/java/com/example/blog/
├── BlogApplication.java
├── common/
│   ├── exception/
│   ├── response/
│   └── config/
├── auth/
│   ├── controller/
│   ├── service/
│   ├── repository/
│   └── entity/
├── post/
│   ├── controller/
│   ├── service/
│   ├── repository/
│   ├── entity/
│   ├── dto/
│   └── mapper/
├── taxonomy/
├── media/
└── markdown/
    ├── parser/
    ├── sanitizer/
    └── exporter/
```

按业务模块组织后端代码，避免一开始建一个包含所有逻辑的巨大 `controller` 或 `service` 包。`frontend` 目录表示独立前端项目位置；前端不是本计划的主要学习主题，但仍需完成公开站点、后台页面和接口联调，并讲清关键实现思路。随着项目变大，再决定是否需要更严格的领域层和应用层。

## 7. 建议的 Git 提交节奏

提交信息应描述用户可感知的变化，例如：

- `chore: 初始化 Spring Boot 项目`
- `feat: 增加文章草稿 CRUD`
- `feat: 增加文章自动保存接口`
- `feat: 增加作者登录和后台权限`
- `feat: 支持 Markdown 导入和 Front Matter 解析`
- `feat: 支持单篇文章导出`
- `test: 增加文章发布流程集成测试`
- `fix: 阻止草稿出现在公开搜索结果`

每次提交前执行：

```bash
mvn test
git diff --check
git status
```

## 8. 每个功能的通用开发流程

1. 从需求文档中写出用户场景和验收条件。
2. 设计数据结构、接口输入输出和异常情况。
3. 先写一个失败测试或明确的手工验证步骤。
4. 编写最小实现，让主流程跑通。
5. 补充校验、权限、日志和边界情况。
6. 写单元测试和接口测试。
7. 从浏览器实际操作一遍。
8. 整理代码、更新 README、提交 Git。

## 9. 阶段性里程碑

### 里程碑 1：能写

完成阶段 0-3。可以启动后端、创建文章、保存到 PostgreSQL，并通过 REST API 返回数据；前端可以完成一次基础联调。

### 里程碑 2：能发布

完成阶段 4-7。文章可以通过 API 安全编辑、预览、发布，Next.js 前端可以通过公开接口完成阅读。

### 里程碑 3：能迁移

完成阶段 8-9。文章有分类、标签、搜索，并可以与本地 Markdown 文件互相导入导出。

### 里程碑 4：能上线

完成阶段 10-11。核心流程有测试，应用可以通过 Docker 部署，并具备备份和恢复能力。

## 10. 学习中的常见陷阱

- 一开始就学习微服务、分布式事务和消息队列，导致博客迟迟不能运行。
- 只会调用 JPA，不理解 SQL、索引和事务。
- 只在前端隐藏管理按钮，没有在后端校验权限。
- 把渲染后的 HTML 当作唯一内容，导致导出和迁移困难。
- 只测试正常流程，不测试重复 slug、空文件、上传恶意文件和网络失败。
- 复制教程代码后不尝试修改，无法形成自己的理解。
- 为了“架构完整”过早创建大量空接口和抽象层。

## 11. 第一周的具体任务

建议今天开始完成以下任务：

1. 安装并确认 `java -version`、`mvn -version`、`git --version`。
2. 创建 Spring Boot 项目并启动成功。
3. 新增 `/api/health` 接口，返回 `UP`。
4. 新增第一个 JUnit 测试。
5. 写 README，记录本机环境、启动方式和已完成内容。
6. 提交第一次 Git commit。

第一周不要实现登录、编辑器和数据库。先确保你理解项目如何启动、代码如何编译、测试如何运行、配置如何加载，以及一次 HTTP 请求如何进入 REST Controller 并返回 JSON 响应。
