# 个人博客产品需求文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 个人博客（暂定名） |
| 文档版本 | v1.0 |
| 文档状态 | 需求初稿 |
| 目标用户 | 个人作者、文章读者 |
| 首期形态 | Web 响应式应用 |
| 核心能力 | 在线编写 Markdown、保存草稿、发布文章、公开阅读 |

## 2. 产品概述

### 2.1 产品定位

一个面向个人作者的轻量博客系统。作者可以像使用飞书文档一样，在浏览器中创建和编辑 Markdown 文章，系统自动保存内容；文章发布后生成稳定的公开链接，任何获得链接的访客都可以阅读。

### 2.2 核心价值

1. 写作入口简单：登录后即可新建文章，不需要本地编辑器、Git 或部署环境。
2. 写作过程可靠：自动保存、草稿状态和版本恢复降低内容丢失风险。
3. 发布流程清晰：作者可预览文章，确认标题、封面、摘要和可见性后再发布。
4. 阅读体验稳定：公开文章具备清晰排版、目录、代码高亮、移动端适配和可分享链接。
5. 内容可沉淀：文章支持分类、标签、搜索和归档，形成可持续维护的个人知识空间。

### 2.3 产品假设

- 首期主要服务一个博客站点的单个站长，不建设复杂的多租户后台。
- 作者通过账号登录后台，访客无需登录即可阅读公开文章。
- Markdown 是内容源格式，发布时渲染为安全的 HTML 或等价展示结构。
- 首期不提供多人协同编辑、复杂评论社区和付费订阅。

## 3. 目标与非目标

### 3.1 首期目标

- 作者可以创建、编辑、保存、预览、发布和下线文章。
- 作者可以维护文章标题、摘要、封面、分类、标签、发布时间和公开状态。
- 访客可以通过首页、归档、分类、标签或文章链接浏览内容。
- 文章在桌面端和移动端均可正常阅读。
- 内容保存、发布、权限和安全校验具备可上线的基本可靠性。

### 3.2 非目标

- 首期不做多人实时协同、评论审核、私密分享、付费墙和会员体系。
- 首期不做原生 App、小程序和离线编辑。
- 首期不提供任意自定义前端代码、脚本或主题市场。
- 首期不承诺兼容所有复杂 HTML、第三方嵌入和非标准 Markdown 扩展。

## 4. 用户角色与权限

| 角色 | 能力 |
| --- | --- |
| 访客 | 查看首页、文章详情、分类、标签、归档、搜索结果；复制和分享公开链接 |
| 作者/管理员 | 登录后台；创建、编辑、预览、发布、下线、删除文章；管理分类、标签、站点信息 |
| 超级管理员 | 首期可与作者合并；后续如扩展多用户再拆分权限 |

权限规则：

- 草稿、已下线和已删除文章仅作者可访问。
- 公开文章允许匿名访问。
- 文章的管理接口必须服务端校验作者身份，不能只依赖前端隐藏按钮。
- 删除优先采用软删除，避免误删后无法恢复；彻底删除可作为后续管理能力。

## 5. 信息架构

### 5.1 公开端

- 首页 `/`
- 文章详情 `/posts/{slug}`
- 归档 `/archive`
- 分类页 `/categories/{slug}`
- 标签页 `/tags/{slug}`
- 搜索页 `/search?q={keyword}`
- 关于页 `/about`
- 404 页面

### 5.2 管理端

- 登录 `/admin/login`
- 控制台 `/admin`
- 文章列表 `/admin/posts`
- 新建文章 `/admin/posts/new`
- 编辑文章 `/admin/posts/{id}/edit`
- 文章预览 `/admin/posts/{id}/preview`
- 分类与标签 `/admin/taxonomy`
- 站点设置 `/admin/settings`

## 6. 核心业务流程

### 6.1 新建并发布文章

1. 作者登录后台，点击“新建文章”。
2. 系统立即创建一条草稿，并进入编辑器。
3. 作者填写标题和 Markdown 正文，系统在内容变更后自动保存。
4. 作者可切换编辑、预览或分栏模式检查效果。
5. 作者点击“发布”，填写或确认摘要、封面、分类、标签、slug、发布时间和可见性。
6. 系统校验必填项和 slug 唯一性，生成发布记录。
7. 发布成功后展示公开链接，作者可直接打开文章页。

### 6.2 继续编辑草稿

1. 作者进入文章列表，按状态筛选“草稿”。
2. 点击文章进入编辑页。
3. 编辑器加载最近一次成功保存的内容。
4. 自动保存状态显示为“已保存”；保存失败时显示“保存失败，点击重试”。

### 6.3 修改已发布文章

1. 作者打开已发布文章并点击“编辑”。
2. 编辑过程不影响当前线上版本。
3. 保存后的内容进入草稿变更区或直接作为未发布修改。
4. 作者点击“更新发布”后，线上版本才替换为新版本。
5. 系统保留上一版本，支持查看版本记录和恢复。

### 6.4 下线文章

1. 作者在文章列表或编辑页点击“下线”。
2. 系统二次确认，并提示下线后公开链接不可访问。
3. 文章状态变为“已下线”，公开端返回 404 或文章不可见页。
4. 作者可再次编辑并重新发布，原 slug 默认保留。

## 7. 功能需求

### 7.1 登录与账号

#### P0

- 支持账号密码登录。
- 登录失败提示明确但不暴露账号是否存在。
- 支持退出登录。
- 登录态具备过期机制；后台页面未登录时跳转到登录页。
- 登录接口具备基础限流和失败次数保护。

#### P1

- 支持邮箱验证码或第三方登录。
- 支持修改密码和找回密码。

### 7.2 管理控制台

控制台展示：

- 文章总数、已发布数、草稿数、已下线数。
- 最近编辑文章。
- 最近发布文章。
- 最近访问量（若接入统计）。
- 快速操作：新建文章、查看博客、进入设置。

文章列表必须支持：

- 按标题搜索。
- 按状态筛选：全部、草稿、已发布、已下线、回收站。
- 按分类、标签筛选。
- 按更新时间或发布时间排序。
- 展示标题、状态、更新时间、发布时间、分类和阅读数。
- 批量操作至少支持批量下线和批量移动到回收站。

### 7.3 Markdown 编辑器

编辑器是产品核心，必须保证内容输入、保存和恢复可靠。

#### 编辑模式

- 纯 Markdown 编辑模式：适合熟悉 Markdown 的作者。
- 实时预览模式：左侧编辑、右侧查看渲染结果。
- 全屏写作模式：隐藏非必要导航，扩大正文区域。
- 桌面端支持拖拽调整编辑区和预览区宽度；移动端改为编辑/预览切换。

#### Markdown 能力

P0 支持：

- 标题、段落、粗体、斜体、删除线。
- 有序列表、无序列表、任务列表。
- 引用、分隔线。
- 链接和图片。
- 行内代码和代码块。
- 表格。
- 数学公式可作为 P1 能力，需明确采用的语法和渲染方案。

#### 编辑器交互

- `Ctrl/Cmd + S` 主动保存。
- `Ctrl/Cmd + Enter` 打开发布面板或执行发布动作，需避免误发布。
- 支持常用 Markdown 快捷键和工具栏按钮。
- 支持粘贴纯文本和 Markdown 文本。
- 图片可通过文件选择或拖拽上传，上传后自动插入 Markdown 图片语法。
- 显示字数、预计阅读时长和当前保存状态。
- 离开编辑页前若存在未保存内容，弹出离开确认。

#### 自动保存

- 新建草稿后立即持久化初始记录。
- 内容变更停止 1 秒后触发保存，连续输入时进行防抖。
- 保存成功显示最近保存时间。
- 保存失败不覆盖本地编辑内容，并提供重试入口。
- 网络短暂中断时保留浏览器本地备份；恢复连接后可继续保存。
- 多窗口编辑时检测版本冲突，提示作者选择覆盖、保留本地内容或查看服务端版本。

#### 内容安全

- 服务端统一解析 Markdown，不直接信任客户端传入的 HTML。
- 对 HTML、脚本、事件属性和危险 URL 做过滤，防止 XSS。
- 图片、链接和嵌入内容限制协议为 `https`、`http` 或站内资源协议，具体按部署环境确定。
- 限制单篇文章大小，建议首期正文上限 2 MB。

### 7.4 Markdown 导入与文章导出

#### Markdown 文件导入

P0 支持作者在文章列表和新建文章页导入本地 `.md` 或 `.markdown` 文件。

- 导入入口可通过“导入文章”按钮进入，也支持将文件拖拽到导入区域。
- 服务端校验文件扩展名、MIME 类型和文件大小；单个 Markdown 文件默认不超过 2 MB。
- 导入文件后解析 Markdown 正文，并尝试读取 Front Matter 元数据。
- 支持 YAML Front Matter，首期识别以下字段：`title`、`slug`、`description`/`excerpt`、`cover`、`date`/`published_at`、`category`、`tags`、`status`。
- 没有标题时，默认使用 Front Matter 的 `title`；仍为空时使用文件名作为标题。
- 没有 slug 时根据标题生成；如果 slug 已存在，自动追加短后缀并提示作者确认。
- 导入内容默认进入“草稿”，不会因为文件中声明了 `published` 或 `status: published` 而直接公开发布。
- 导入后进入编辑页，作者可以检查解析结果、补充属性并手动发布。
- 导入失败时说明具体原因，例如格式无法解析、文件超限、编码异常或字段类型不正确。
- 默认按 UTF-8 读取；无法识别的编码应提示作者转换后重新导入，不得静默乱码。
- 图片引用保留原始 Markdown 路径。相对路径图片若未同步上传，预览中提示资源不可用；支持将本地图片一并导入作为后续增强能力。
- 导入不会覆盖已有文章；如作者选择导入到已有文章，必须二次确认并生成新的未发布版本。

#### 文章导出

P0 支持作者从文章列表和文章编辑页导出文章。

- 单篇文章支持导出为 `.md` 文件，下载内容以 Markdown 原文为主。
- 导出文件默认包含 Front Matter，至少包括 `title`、`slug`、`excerpt`、`cover`、`category`、`tags`、`published_at`、`status` 和 `updated_at`。
- 导出文件名默认使用 `slug.md`；文件名包含非法字符时自动替换，避免下载失败。
- 导出内容使用 UTF-8 编码，保留标题、正文、代码块、表格、链接、图片引用和换行结构。
- 导出草稿时必须保留当前编辑中的最新已保存版本，并提示尚未保存的本地修改不会自动包含在导出文件中。
- 对有权限访问的文章才显示导出入口；访客不能通过公开页面导出草稿或管理元数据。
- 文章列表支持批量导出作为 P1，建议导出为 ZIP，单篇导出作为首期唯一交付范围。
- 导出后的 Markdown 应能够再次导入，并通过校验后恢复标题、正文和主要元数据。

#### 导入导出验收规则

- 导入一篇带 Front Matter 的 Markdown 后，标题、摘要、slug、分类、标签和发布时间能正确回填。
- 导入文件不会直接发布，作者必须显式点击发布。
- 导出后重新导入同一文件，正文内容和主要元数据不发生不可接受的丢失。
- 重复 slug、非法日期、未知字段和过长字段不会导致整个页面崩溃；系统应保留可解析内容并给出提示。
- 导入和导出操作记录作者、文章和时间，失败记录错误原因，便于排查。

### 7.5 文章属性

文章编辑页支持以下字段：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 标题 | 是 | 公开展示标题；建议 1-100 个字符 |
| 正文 | 是 | Markdown 原文；不能为空 |
| 摘要 | 否 | 用于首页卡片、SEO 描述；未填写时自动截取正文 |
| 封面图 | 否 | 用于首页卡片和社交分享图；支持上传或图片 URL |
| slug | 是 | 文章 URL 唯一标识；默认由标题生成，允许手动修改 |
| 分类 | 否 | 单选；首期允许未分类 |
| 标签 | 否 | 多选；支持创建新标签 |
| 发布时间 | 是 | 默认当前时间；可定时发布作为 P1 |
| 可见性 | 是 | 公开、仅自己可见；私密链接作为 P1 |
| SEO 标题 | 否 | 默认使用文章标题 |
| SEO 描述 | 否 | 默认使用摘要 |

### 7.6 预览与发布

- 预览页使用与公开端一致的渲染组件和样式，避免所见不一致。
- 预览不改变线上文章状态。
- 发布前检查标题、正文、slug、可见性等必填项。
- 发布面板展示文章摘要信息和公开链接预览。
- 首次发布按钮文案为“发布”，已发布文章修改后按钮文案为“更新发布”。
- 发布成功显示成功提示和“查看文章”入口。
- 发布失败保留编辑内容，显示可理解的错误原因。

### 7.7 公开首页

首页包含：

- 站点名称、头像或 Logo、简介。
- 最新文章列表，按发布时间倒序。
- 每篇文章展示标题、摘要、发布时间、分类、标签和阅读时长。
- 分页或“加载更多”；首期建议分页，便于 SEO 和稳定加载。
- 置顶文章区域作为 P1。
- 归档、分类、标签、关于页入口。
- 搜索入口。

空状态：尚未发布文章时显示站点简介和明确的空内容状态；作者登录后可看到新建文章入口。

### 7.8 文章详情页

- 展示标题、摘要、作者、发布时间、更新时间、分类、标签和正文。
- 正文支持标题层级、代码高亮、图片自适应、表格横向滚动、引用和链接样式。
- 根据 H2/H3 自动生成目录；移动端目录改为可展开区域。
- 支持复制代码块、文章链接复制、浏览器原生分享。
- 文章底部展示上一篇/下一篇或返回归档入口。
- 公开页面不展示后台编辑控件。
- 文章不存在、已下线或无权限时返回正确状态码和友好页面。

### 7.9 搜索、分类与标签

- 搜索标题、摘要和正文，默认按相关性和发布时间综合排序。
- 搜索结果展示命中标题、摘要和发布时间。
- 无结果时显示关键词和清晰空状态。
- 分类页展示分类名称、简介和该分类文章列表。
- 标签页展示标签下文章列表。
- 分类和标签 slug 必须唯一且稳定。

### 7.10 站点设置

P0 设置项：

- 站点名称、站点副标题、站点简介。
- 作者头像、作者名称、关于页内容。
- 首页每页文章数。
- 时区、默认语言和默认发布时间规则。
- favicon 和默认分享图。

P1 设置项：

- 自定义域名。
- 自定义主题色和字体配置。
- RSS 地址。
- 评论开关和第三方评论配置。

### 7.11 图片与媒体

- 支持常见图片格式：JPEG、PNG、GIF、WebP；单文件大小默认不超过 10 MB。
- 上传过程中展示进度，失败时允许重试。
- 服务端校验文件类型和大小，不能只依赖文件扩展名。
- 图片生成稳定资源 URL，并在文章删除时保留资源或进入清理队列，避免历史版本失效。
- 首期不支持视频托管；可允许粘贴受控的外部视频链接作为 P1。

## 8. 状态与规则

### 8.1 文章状态

| 状态 | 说明 | 公开端可见 |
| --- | --- | --- |
| 草稿 | 已创建但从未发布，或存在未发布修改 | 否 |
| 已发布 | 当前版本对外公开 | 是 |
| 已下线 | 曾发布但当前不公开 | 否 |
| 回收站 | 作者标记删除，可恢复 | 否 |

状态转换：

- 新建 -> 草稿。
- 草稿 -> 已发布。
- 已发布 -> 已发布（更新发布）。
- 已发布 -> 已下线。
- 草稿/已下线 -> 回收站。
- 回收站 -> 草稿或已下线，恢复后根据历史状态决定。

### 8.2 slug 规则

- slug 在站点内唯一，长度建议 1-120 个字符。
- 默认由标题生成；中文标题可生成拼音 slug 或稳定短 ID，产品需统一方案。
- 已发布文章修改 slug 时必须二次确认。
- P1 支持旧 slug 301 重定向到新 slug，避免外部链接失效。

### 8.3 发布时间规则

- 文章未发布时可以修改计划发布时间。
- 首期发布即生效，定时发布作为 P1。
- 更新文章默认不改变首次发布时间，只更新最后修改时间。
- 公开端默认按发布时间排序。

## 9. 数据模型建议

### 9.1 User

- `id`
- `email` 或登录名
- `password_hash`
- `display_name`
- `avatar_url`
- `role`
- `created_at`
- `updated_at`
- `last_login_at`

### 9.2 Post

- `id`
- `author_id`
- `title`
- `slug`
- `excerpt`
- `cover_url`
- `markdown_content`
- `rendered_content`（可选，可按需缓存）
- `status`
- `visibility`
- `category_id`
- `published_at`
- `updated_at`
- `deleted_at`
- `view_count`
- `version`

### 9.3 PostVersion

- `id`
- `post_id`
- `version_number`
- `title`
- `markdown_content`
- `saved_by`
- `created_at`
- `change_note`（可选）

### 9.4 Category / Tag / Media / SiteSetting

- Category：`id`、`name`、`slug`、`description`、`sort_order`、`created_at`。
- Tag：`id`、`name`、`slug`、`created_at`；文章与标签使用关联表。
- Media：`id`、`url`、`mime_type`、`size`、`width`、`height`、`uploader_id`、`created_at`。
- SiteSetting：`key`、`value`、`updated_at`。

## 10. 非功能需求

### 10.1 性能

- 公开首页和文章详情首屏加载目标：良好网络下 LCP 小于 2.5 秒。
- 文章保存接口正常情况下 1 秒内返回。
- 公开内容应支持缓存；文章更新后相关缓存及时失效。
- 图片使用压缩、懒加载和合适尺寸，避免大图拖慢首屏。

### 10.2 可用性

- 公开端移动端宽度 320 px 起可用。
- 编辑器在常见桌面浏览器中可用，移动端支持基本编辑和保存。
- 颜色对比、键盘焦点、按钮状态和错误提示符合基本无障碍要求。
- 关键操作有明确成功、失败、加载和空状态。

### 10.3 安全

- 密码使用强哈希算法存储，不保存明文。
- 使用安全 Cookie 或等价令牌机制，并防护 CSRF、XSS 和越权访问。
- Markdown 渲染结果必须经过白名单过滤。
- 上传接口校验 MIME、扩展名、大小和内容类型。
- 管理端接口记录关键操作日志：登录、发布、下线、删除、恢复。
- 备份数据库和文章内容，至少支持按日恢复。

### 10.4 SEO 与分享

- 每篇公开文章有唯一 title、description 和 canonical URL。
- 生成 sitemap.xml 和 robots.txt。
- 公开文章支持 Open Graph 和 Twitter Card 元信息。
- 语义化 HTML，文章标题层级正确。
- 公开文章使用可抓取的服务端渲染或静态生成方案，具体由技术方案确定。

## 11. 埋点与运营指标

首期至少记录：

- 登录成功/失败。
- 新建文章。
- 自动保存成功/失败。
- 预览。
- 发布成功/失败。
- 下线和删除。
- 首页访问、文章详情访问、搜索、复制文章链接。

核心指标：

- 发布文章数。
- 草稿保存成功率。
- 从新建到发布的转化率。
- 公开文章访问量和独立访客数。
- 搜索无结果率。
- 编辑器保存失败率。

## 12. MVP 范围与版本规划

### MVP / P0

- 登录与退出。
- 管理控制台和文章列表。
- Markdown 编辑、预览、自动保存。
- 文章基础属性。
- 发布、更新发布、下线、回收站。
- 公开首页和文章详情页。
- 分类、标签、归档和基础搜索。
- 图片上传。
- Markdown 文件导入和单篇文章导出。
- 响应式布局、基础 SEO 和安全过滤。

### P1

- 定时发布。
- 私密链接。
- 版本对比和恢复。
- RSS/Atom。
- 自定义域名和主题配置。
- 评论或第三方评论。
- 访问统计报表。
- 数学公式、Mermaid、外部视频等扩展 Markdown。

### P2

- 多作者和协作权限。
- 实时协同编辑。
- 邮件订阅。
- 多语言和多站点。
- 主题市场或自定义模板。

## 13. 关键验收标准

### 写作与保存

- 作者新建文章后，关闭并重新打开页面，标题和正文仍完整存在。
- 连续输入时不会因频繁请求造成明显卡顿。
- 保存失败时本地内容不丢失，用户可以重试。
- 预览中的标题、列表、表格、代码、图片与公开页面渲染一致。
- 导入 Markdown 后内容以草稿打开，Front Matter 中的主要元数据正确回填。
- 导出文章后得到 UTF-8 编码的 `.md` 文件，重新导入后正文和主要元数据可恢复。
- 导入非法或超限文件时有明确错误提示，且不会覆盖已有文章。

### 发布与权限

- 标题或正文为空时无法发布，并定位到错误字段。
- 重复 slug 无法发布，错误提示清晰。
- 发布成功后可从公开链接匿名打开文章。
- 草稿和下线文章使用公开链接访问时不可见。
- 未登录用户无法调用文章管理接口。

### 阅读与兼容

- 首页能按发布时间正确展示已发布文章。
- 文章详情页在桌面端、手机端无横向溢出；代码块和表格可滚动。
- 浏览器刷新、复制链接和前进后退不会破坏页面状态。
- 文章有正确的页面标题、描述和分享元信息。

## 14. 技术实现建议

技术选型可根据团队熟悉度决定，但建议遵循以下边界：

- 前端使用成熟 Markdown 编辑器内核，避免自行实现解析器。
- Markdown 解析和 HTML 清洗必须在服务端完成或至少服务端复核。
- 内容源保存 Markdown，渲染结果作为缓存而非唯一事实来源。
- 数据库保存文章状态和版本；对象存储保存图片。
- 公开端优先采用 SSR、SSG 或缓存友好的方案，以满足 SEO 和加载性能。
- 认证、发布、媒体上传和渲染接口都需有自动化测试。

## 15. 风险与待确认事项

1. 中文标题对应的 slug 方案：拼音、中文 URL 还是短 ID。
2. 是否允许作者输入原始 HTML；建议首期关闭或严格白名单过滤。
3. 图片存储位置、容量、CDN 和备份策略。
4. 是否需要评论，以及评论由自建系统还是第三方服务提供。
5. 是否需要自定义域名、RSS 和统计；这些会影响部署与 SEO 方案。
6. 文章删除后的资源清理、版本保留周期和备份周期。
7. 是否需要多作者；若需要，应在数据模型中从首期保留 author_id 和权限边界。
8. 是否需要私密文章；“仅自己可见”和“获得链接可见”应分开定义。

## 16. 建议的首期交付顺序

1. 设计数据模型、认证和文章状态机。
2. 完成管理端文章列表、新建和编辑器。
3. 完成自动保存、预览、发布和下线。
4. 完成公开首页、文章详情、归档、分类和标签。
5. 接入图片上传、搜索、SEO 和安全过滤。
6. 做桌面端与移动端适配、异常流程测试和上线前数据备份。
